// Toggle visual debugging aids
// When true, highlights the selected anchor block in previews/hover shadows
export const DEBUG_ANCHOR = true; // set to false to disable quickly

// When true, shows a small on-screen debug window with shadow coordinates
// and anchor indices to help verify mobile drag/drop alignment.
export const DEBUG_OVERLAY = true; // set to false to hide
